# Release assets

Attach `square-riesz-polarization-certificates-v1.0.0.zip` and its `.sha256` file to the GitHub `v1.0.0` Release. The ZIP is generated outside the repository to avoid recursive packaging.
