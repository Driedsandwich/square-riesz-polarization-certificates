name: smoke-test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary audit-remediation branch state; final smoke-test workflow will be restored before merge."
